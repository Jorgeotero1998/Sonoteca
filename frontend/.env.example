VITE_API_BASE_URL=http://localhost:8000

# If you run the backend on a different port (e.g. 8001), update this value.

# Deezer-first: no keys required (public API).
# Spotify is optional and configured only on backend.

